spring:
  application:
    name: hr-config-server
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/Severianofsp/ms-cloud-config
server:
  port: 8888
